/*global describe it*/
import assert from 'assert';
import createSVGPie from '../src/createSVGPie.js';

describe('createSVGPie()', function () {
    it('finds that true is true.', function () {
        assert.equal(true, true);
    });
});